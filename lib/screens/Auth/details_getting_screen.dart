import 'package:flutter/material.dart';

class DetailsGettingScreen extends StatelessWidget {
  const DetailsGettingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
